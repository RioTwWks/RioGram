# Контекст проекта — RioGram

**Название:** RioGram  
**Цель:** Кросс-платформенный клиент Telegram (Flutter + модифицированный TDLib) с обходом DPI РКН.

## Проблема

С 2025–2026 РКН блокирует MTProto и SOCKS5. Fake TLS на прокси недостаточен: DPI анализирует TLS ClientHello, fingerprints и паттерны трафика. Нужен форк клиента + каскадная прокси-схема.

## Архитектура (native)

```
Flutter UI (Dart)
    ↓ FFI / tdweb (Web)
TDLib (C++, патчи DPI_BYPASS: ClientHello, фрагментация, DRS)
    ↓
PhantomProxy (основной, RU VPS) ──failover──► StealthGate Front (резерв, RU VPS)
                                                      ↓ SGFB-туннель
                                              StealthGate Back (EU VPS)
                                                      ↓
                                              Telegram DC
```

## Архитектура (Web, §8)

В браузере нет FFI/Fake TLS — MTProto туннелируется через **WSS** и схему **RU Frontend → EU Backend**:

```
Browser (Flutter Web + tdweb WASM)
    ↓ wss://your-domain.ru/venus.web.telegram.org/apiws
RU VPS (Nginx TLS :443) → SSH reverse tunnel :8080
    ↓
EU VPS nginx :8080 (aggregator)
    ├─ /opt/riogram/web/          Flutter static (§8.5)
    └─ riogram-wss-proxy :5001     → venus/pluto.web.telegram.org
```

| Компонент | Репозиторий / путь |
|-----------|-------------------|
| WSS hook + tdweb bridge | `web/js/`, `lib/core/tdlib/tdlib_client_web.dart` |
| WSS reverse proxy | `server/wss-proxy/` → `bin/riogram-wss-proxy` |
| Инфра RU/EU | `deploy/nginx/`, `scripts/setup-web-infra-*.sh` |
| E2E | `scripts/run-web-e2e.sh`, `e2e/web/` |

Документация: `docs/WEB.md`, `WEB_TRANSPORT.md`, `WEB_PROXY.md`, `WEB_INFRA.md`, `WEB_E2E.md`.  
Команды Cursor: `.cursor/commands/web.md`.

## Прокси-стратегия

| Прокси | Роль | Репозиторий |
|--------|------|-------------|
| **PhantomProxy** (Go) | Основной, стабильный MTProto-прокси | github.com/RioTwWks/PhantomProxy |
| **StealthGate** (Rust) | Резервный, Front/Back Split (RU→EU) | github.com/RioTwWks/StealthGate |

- **Не подключаться напрямую к EU** — только через RU-посредник.
- **Failover:** TDLib `addProxy` → `pingProxy` → `enableProxy` (PhantomProxy первый, StealthGate второй).
- **Transport proxy:** системный HTTP/SOCKS через `RioGram:Transport` в `ConnectionCreator.cpp`.

## TDLib (форк + upstream)

- Каталог: `td/` (vendor, не submodule)
- База upstream: **`td/upstream-base.json`** (сейчас v1.8.66)
- Патчи DPI: `docs/TDLIB_PATCHES.md` — все изменения с `// DPI_BYPASS:`
- Merge upstream: `docs/TDLIB_UPSTREAM_SYNC.md`, `./scripts/check-tdlib-upstream.sh`
- CI: `.github/workflows/tdlib-upstream-sync.yml` (еженедельно → Issue при новой версии)
- Команда Cursor: `.cursor/commands/tdlib-upstream.md`, `.cursor/commands/build-tdlib.md`

## Flutter-стек

- FFI: `TdlibClient` + `tdlib_bindings.dart`; Web — `tdlib_client_web.dart`
- **Provider:** `AuthManager`, `ChatManager`, `ProxyManager`, `ThemeManager`, `PluginManager`, …
- Структура: `lib/core/`, `lib/screens/`, `lib/widgets/`, `lib/models/`
- Дизайн §9: `lib/core/theme/telegram_theme.dart` (`TelegramTheme`, `TelegramColors`)

### Список чатов (§6.2 ✅)

- `ChatManager`: `loadChats`, pin/archive, поиск, delete, mark unread
- Адаптивный UI: mobile / master-detail (720px) / 3-column (840px)
- Документация: `docs/CHATS.md`

### RioGram-фичи (§7)

| Блок | Статус | Документация |
|------|--------|--------------|
| §7.2 Призрачный режим, анти-отзыв, медиа | ✅ | `TODO.md` §7.2 |
| §7.3 UI-кастомизация сверх §9 | ✅ | настройки `ui_customization_*` |
| §7.4 Local Premium, AdBlock, телеметрия | ✅ | `security_privacy_*` |
| §7.5 Mini Apps, LaTeX, автопостинг | ✅ | `docs/PLUGINS.md` (LaTeX в chat/) |
| §7.6 Плагины + децентрализация (research) | ✅ | `docs/PLUGINS.md`, `DECENTRALIZATION.md` |
| §7.7 Sync с upstream TDLib | ✅ | `TDLIB_UPSTREAM_SYNC.md` |

Плагины: `PluginManager`, `RioGramPlugin`, hooks в `ChatManager` / `MessageBubble`.

## Сервера (не коммитить реальные IP)

- VPS РФ #1: PhantomProxy
- VPS РФ #2: StealthGate Front
- VPS EU: StealthGate Back + Web backend

## Секреты

- `api_id`, `api_hash` — только в `.env` / GitHub Actions Secrets
- Прокси-секреты — в `.env` или Secrets, не в коде
- `docs/SECRETS.md`, `docs/SIGNING.md`

## CI/CD

| Workflow | Назначение |
|----------|------------|
| `ci.yml` | flutter analyze/test, tdlib-linux, flutter-web, web-e2e, flutter-linux |
| `release.yml` | пакеты всех платформ при GitHub Release |
| `tdlib-upstream-sync.yml` | проверка новой версии tdlib/td → Issue |

Локально: `./scripts/ci-flutter.sh`, `./scripts/run-web-e2e.sh`, `./scripts/check-tdlib-upstream.sh`  
Документация: `docs/CI.md`

## Текущий статус (кратко)

- **Этапы 1–5 MVP** ✅ (Flutter, TDLib, DPI, прокси, UI, сборки, Release)
- **§6** ⏳ функциональный паритет — в работе (§6.2 ✅, далее сообщения/медиа/группы)
- **§7.3–§7.7** ✅
- **§8 Web** ✅ PoC + WSS + infra + E2E; ручной деплой ops — открыт
- **§9** ✅ классический дизайн Telegram (токены §9.1–§9.10, pixel parity §9.11); docs: `docs/DESIGN.md`
- **§9.11.9** ⏳ ручная side-by-side — `docs/SIDE_BY_SIDE_CHECKLIST.md`; widget-регрессия ✅

Актуальный чеклист: `TODO.md`, план: `PLAN.md`.

## Дизайн §9 (классический Telegram)

| Компонент | Путь |
|-----------|------|
| Токены, тема | `lib/core/theme/telegram_theme.dart` — `TelegramColors`, `TelegramSpacing`, `TelegramThemeData`, `TelegramDesignConstraints` |
| Иконки | `lib/core/theme/telegram_icons.dart` — `TelegramIcons` (outline 24dp) |
| Фон переписки | `lib/widgets/chat_wallpaper.dart` — `ChatWallpaper` |
| Настройки UI | `lib/widgets/telegram_settings_tile.dart` |
| Список чатов | `lib/widgets/chat_list_tile.dart` (row 72px) |
| Пузыри | `lib/widgets/message_bubble.dart` |
| Ввод | `lib/widgets/message_input_bar.dart` |

**Правила:** `context.telegramTheme` вместо хардкода цветов; **запрет** `BackdropFilter`/blur на панелях; flat shell (elevation 0). Кастомизация §7.3 поверх §9 — `ThemeManager`.

**Документация:** `docs/DESIGN.md`, `docs/RELEASE_NOTES_1.1.0.md`, `docs/SIDE_BY_SIDE_CHECKLIST.md`, `docs/APP_ICON.md`.

**Тесты:** `flutter test test/telegram_refinement_test.dart` + связанные `telegram_*_test.dart`, `chat_list_tile_test.dart`, `message_bubble_test.dart`, `desktop_layout_regression_test.dart`.

**Cursor:** `.cursor/commands/telegram-design.md`, правила в `.cursor/rules/flutter.mdc`.

## Лицензия

GPLv3 with OpenSSL exception (производный от Telegram Desktop / TDLib).
