Краткое описание проекта, чтобы AI всегда помнил ключевые детали. Содержимое:
```
# Контекст проекта — Telegram Client MVP

**Название:** YourTelegramClient (кодовое имя YTC)
**Цель:** Создать кросс-платформенный клиент Telegram с обходом DPI.

**Стек:**
- Flutter (Dart) для UI
- TDLib (C++) модифицированный для Fake TLS, фрагментации
- Прокси: PhantomProxy (основной) и StealthGate (резервный, сплит-режим)

**Сервера:**
- VPS РФ #1: 178.xxx.xxx.xxx:443 (PhantomProxy)
- VPS РФ #2: 185.xxx.xxx.xxx:443 (StealthGate Front)
- VPS EU: 141.xxx.xxx.xxx:443 (StealthGate Back)

**API ключи:** хранятся в .env (не коммитить)

**Текущий статус:** разработка MVP, этап модификации TDLib.

**Важно:** при любом предложении кода проверять, не нарушает ли он безопасность и не светит ли секреты.
```
