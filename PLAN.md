# PLAN.md — План разработки MVP кросс-платформенного клиента Telegram

## 1. Цель
Создать собственный кросс-платформенный (Windows, macOS, Linux, Android, iOS) клиент Telegram на Flutter с модифицированным ядром TDLib, способным обходить блокировки РКН с помощью:
- рандомизированного TLS ClientHello (Fake TLS)
- фрагментации и маскировки трафика
- поддержки собственных прокси (PhantomProxy и StealthGate) с автоматическим переключением между ними.

Приложение должно давать полный контроль над кастомизацией интерфейса и сетевой логикой.

---

## 2. Архитектура

```
┌─────────────────────────────────────────────────────┐
│                Flutter UI (Dart)                    │
│  - кастомизируемые темы, виджеты, анимации         │
│  - экраны авторизации, чатов, настроек              │
└─────────────────────┬───────────────────────────────┘
                      │ FFI (Dart ↔ C++)
┌─────────────────────▼───────────────────────────────┐
│              TDLib (модифицированный)                │
│  - стандартная логика Telegram API                   │
│  - ПАТЧИ для обхода DPI:                            │
│    • рандомизация ClientHello (под Chrome/Firefox)  │
│    • фрагментация первого пакета                    │
│    • варьирование размеров TLS-записей             │
│    • подмена fingerprint TLS-стека                  │
└─────────────────────┬───────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────┐
│     Прокси-серверы (на ваших VPS)                   │
│  - Основной: PhantomProxy (Go) на VPS в РФ          │
│  - Резервный: StealthGate (Rust) на VPS в РФ       │
│  - Backend (EU): StealthGate (опционально)          │
└─────────────────────────────────────────────────────┘
```

---

## 3. Этапы разработки (MVP)

### Этап 0: Подготовка окружения и инфраструктуры
| Задача | Детали | Оценка времени |
|--------|--------|----------------|
| Настроить VPS в РФ (2 шт.) | Установить Docker / системные пакеты, открыть порты | 1 день |
| Развернуть PhantomProxy | Собрать из исходников или скачать бинарник, запустить с конфигом | 0.5 дня |
| Развернуть StealthGate (Front/Back) | Настроить сплит-режим: Front на РФ, Back на EU | 1 день |
| Проверить работоспособность прокси | Подключиться через официальный клиент вручную | 0.5 дня |
| Получить API credentials | Зарегистрировать приложение на my.telegram.org → api_id, api_hash | 0.5 дня |
| Установить Flutter SDK | Версия ≥3.22, настроить Android/iOS/Desktop окружения | 1 день |
| Установить инструменты сборки TDLib | CMake, GCC/Clang, Ninja, зависимости (OpenSSL, zlib) | 0.5 дня |

### Этап 1: Создание базового Flutter-проекта и интеграция TDLib
| Задача | Детали | Оценка времени |
|--------|--------|----------------|
| Создать проект | `flutter create --org com.yourcompany your_telegram_client` | 0.5 дня |
| Добавить зависимости | `tdlib_library` (или ручной FFI-биндинг), `provider`/`riverpod` для стейта, `dio` для HTTP (опционально) | 0.5 дня |
| Написать обёртку TDLib | Класс `TdlibClient` с инициализацией, отправкой команд, обработкой обновлений | 2 дня |
| Реализовать авторизацию | Поток: запрос номера → код → пароль (если есть) → получение `authorizationState` | 1 день |
| Сделать простой UI | Экран ввода номера, экран ввода кода, список чатов (базовый) | 2 дня |
| Интегрировать логирование | Включить логи TDLib для отладки | 0.5 дня |

**Итог этапа:** Приложение может авторизоваться и показать список чатов (без модификаций обхода).

### Этап 2: Модификация TDLib для обхода DPI
| Задача | Детали | Оценка времени |
|--------|--------|----------------|
| Склонировать исходники TDLib | `git clone https://github.com/tdlib/td.git` | 0.5 дня |
| Изучить сетевой стек | Найти файлы: `td/telegram/NetQuery.cpp`, `td/telegram/Transport.h`, `td/net/HttpProxy.cpp` и др. | 1 день |
| Реализовать рандомизацию ClientHello | Вдохновиться кодом **ZaStoGram** и **TeleMT**:<br>- Создать массив профилей (Chrome, Firefox, Yandex, Safari)<br>- При каждом подключении выбирать случайный профиль<br>- Генерировать случайные параметры (CipherSuites, Extensions) | 2 дня |
| Добавить фрагментацию пакетов | Разбивать первый TLS-пакет на несколько частей (имитация TCP-сегментации) | 1 день |
| Варьировать размеры TLS-записей | Изменять размеры последующих записей в пределах допустимого | 1 день |
| Подмена TLS-стека | Использовать библиотеку `utls` (если возможно) или вручную менять fingerprint | 1 день |
| Собрать модифицированный TDLib | Собрать под все платформы (или хотя бы под Windows и Android для теста) | 2 дня |
| Протестировать с прокси | Подключиться через PhantomProxy и StealthGate, убедиться в прохождении блокировок | 1 день |

**Итог этапа:** Трафик клиента не распознаётся DPI как Telegram, прокси работают стабильно.

### Этап 3: Интеграция прокси и автоматическое переключение
| Задача | Детали | Оценка времени |
|--------|--------|----------------|
| Добавить поддержку прокси в TDLib | Использовать встроенные методы `addProxy`, `enableProxy`, `removeProxy` | 0.5 дня |
| Настроить два прокси | Добавить PhantomProxy как основной (приоритетный), StealthGate как резервный | 0.5 дня |
| Реализовать проверку доступности | Использовать `pingProxy` с таймаутом 5 секунд | 1 день |
| Написать логику переключения | При ошибке соединения или таймауте – переключить на следующий прокси в списке | 1 день |
| Добавить индикацию текущего прокси в UI | Отображать иконку/статус в настройках или на главном экране | 0.5 дня |
| Сделать возможность ручного переключения | Экран настроек со списком прокси и возможностью включить/отключить | 1 день |

**Итог этапа:** Клиент автоматически переключается на рабочий прокси при недоступности основного.

### Этап 4: Полировка интерфейса и кастомизация
| Задача | Детали | Оценка времени |
|--------|--------|----------------|
| Разработать систему тем | Поддержка светлой/тёмной темы, возможность выбора акцентного цвета | 2 дня |
| Добавить базовые функции чата | Отправка текстовых сообщений, просмотр медиа (фото, видео) | 3 дня |
| Добавить уведомления | Локальные push-уведомления (через `flutter_local_notifications`) | 2 дня |
| Реализовать настройки | Экран с возможностью смены прокси, темы, выхода из аккаунта | 1 день |
| Сделать адаптивный дизайн | Под разные размеры экранов (мобильные и десктопные макеты) | 2 дня |

**Итог этапа:** Приложение имеет полноценный, приятный интерфейс и базовые функции чата.

### Этап 5: Сборка и тестирование на всех платформах
| Задача | Детали | Оценка времени |
|--------|--------|----------------|
| Сборка Windows | `flutter build windows` – проверить работоспособность | 1 день |
| Сборка macOS | `flutter build macos` – проверить (требуется Mac) | 1 день |
| Сборка Linux | `flutter build linux` – проверить | 0.5 дня |
| Сборка Android | `flutter build apk` – проверить на эмуляторе и реальном устройстве | 1 день |
| Сборка iOS | `flutter build ios` – проверить (только на Mac) | 1 день |
| Написать тесты | Юнит-тесты для логики прокси, интеграционные тесты для авторизации | 2 дня |
| Исправить найденные баги | Сбор обратной связи от тестирования | 2 дня |

**Итог этапа:** Приложение готово к релизу на всех платформах.

---

## 4. Ресурсы и инструменты

### Репозитории для вдохновения
- **Модификации TDLib:**
  - [ZaStoGram_desktop](https://github.com/youtubediscord/ZaStoGram_desktop) – реализация Fake TLS, фрагментации.
  - [TeleMT/tdlib-obf](https://github.com/telemt/tdlib-obf) – продвинутая обфускация.
- **Flutter-клиенты:**
  - [Mithka](https://github.com/iebb/mithka) – полноценный клиент на Flutter + TDLib.
  - [telega2](https://github.com/festeh/telega2) – пример интеграции TDLib с FFI.
- **Прокси:**
  - Ваши собственные – [StealthGate](https://github.com/RioTwWks/StealthGate) и [PhantomProxy](https://github.com/RioTwWks/PhantomProxy) – использовать как серверную часть.

### Библиотеки и пакеты Flutter
- `tdlib_library` – упрощает интеграцию TDLib (можно форкнуть и заменить на свою сборку).
- `provider` или `riverpod` – управление состоянием.
- `dio` – для HTTP-запросов (если понадобится).
- `flutter_local_notifications` – для уведомлений.
- `shared_preferences` – для хранения настроек.
- `path_provider` – для путей к файлам базы.

### Сборка TDLib
Официальная инструкция: [tdlib.github.io/td/build.html](https://tdlib.github.io/td/build.html)

---

## 5. Примерные сроки

| Этап | Срок |
|------|------|
| Этап 0 – Подготовка | 4–5 дней |
| Этап 1 – Базовый Flutter + TDLib | 6–7 дней |
| Этап 2 – Модификация TDLib (обход DPI) | 8–10 дней |
| Этап 3 – Прокси и автоматическое переключение | 3–4 дня |
| Этап 4 – Полировка UI и кастомизация | 8–10 дней |
| Этап 5 – Сборка и тестирование | 5–7 дней |
| **Итого** | **~6–7 недель** (при полной занятости) |

---

## 6. Риски и их смягчение

| Риск | Смягчение |
|------|-----------|
| Модификации TDLib перестанут работать после обновления протокола | Вести собственный форк TDLib и следить за обновлениями официального репозитория. |
| Блокировка IP-адресов ваших VPS | Использовать несколько VPS в разных подсетях, а также схему Front/Back (StealthGate) – если заблокируют Front, быстро заменить его. |
| Сложность сборки TDLib под все платформы | Использовать CI/CD (GitHub Actions) для автоматической сборки. |
| Ошибки в реализации DPI-обхода | Тщательно тестировать на реальных сетях провайдеров, использовать логи DPI-симуляторов (например, `dpi-tools`). |
| Утечка API-ключа | Хранить `api_hash` не в коде, а получать через защищённый бэкенд или генерировать динамически. |

---

## 7. Критерии готовности MVP

- [ ] Авторизация по номеру телефона работает на всех платформах.
- [ ] Список чатов загружается.
- [ ] Текст сообщений отправляется и принимается.
- [ ] Подключение через PhantomProxy и StealthGate стабильно (без разрывов).
- [ ] При падении основного прокси автоматически переключается на резервный.
- [ ] Приложение имеет настраиваемую тему и базовые настройки.
- [ ] Приложение собрано и запускается на Windows, macOS, Linux, Android, iOS (хотя бы в отладочном режиме).

---

## 8. Дальнейшее развитие (после MVP)

- Добавление голосовых/видеозвонков.
- Полноценная поддержка медиа (документы, стикеры, голосовые сообщения).
- Кастомные стикерпаки и темы интерфейса.
- Встроенный менеджер загрузок.
- Поддержка нескольких аккаунтов.
- Улучшение производительности и оптимизация батареи.

---

**Примечание:** Данный план является динамическим – вы можете корректировать этапы в зависимости от возникающих сложностей. Удачи в разработке!
